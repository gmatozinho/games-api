{
  "name": "delete-test-01",
  "description": "delete-test-01-description",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "knex": "^0.95.6",
    "knex-aurora-data-api-client": "^1.5.5",
    "knexnest": "^1.0.0",
    "pg": "^8.6.0"
  },
  "devDependencies": {
    "aws-sdk": "^2.799.0",
    "jest": "^26.6.3"
  },
  "scripts": {
    "test": "jest --roots __tests__/integration",
    "infra-test": "jest --roots __tests__/infra",
    "start-local": "sam local start-api -p 9999 -t ./template.yml -n ./config.json --docker-network host --debug"
  }
}
